<%= form_for(@picture) do |f| %>
  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit 'Make a friend' %>
  </div>

<% end %>
